<div class="container">
  <table>
    <thead>
      <tr>
        <th><%= t ".id" %></th>
        <th><%= t ".name" %></th>
        <th><%= t ".email" %></th>
        <th><%= t ".action" %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @users %>
    </tbody>
    <%= paginate @users, theme: "twitter-bootstrap-4" %>
  </table>
</div>
